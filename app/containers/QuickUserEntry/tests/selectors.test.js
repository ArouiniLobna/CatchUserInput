// import { selectQuickUserEntryDomain } from '../selectors';

describe('selectQuickUserEntryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
